// import { computed, observable } from 'mobx'


// export const box = (target: any, name: string, describe?: string) => {
//   const privateName = `_${name}`
//   observable(target, privateName, describe);

//   return computed(target, name, {

//   })
// }